import React from 'react';

export default function AdminDashboard() {
    return (
        <div>
            <h1>Admin Dashboard (React)</h1>
            <p>Welcome, Admin! Manage users, API keys, and analytics.</p>
            {/* Add React components for management and analytics */}
        </div>
    );
} 