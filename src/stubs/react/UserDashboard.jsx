import React from 'react';

export default function UserDashboard() {
    return (
        <div>
            <h1>User Dashboard (React)</h1>
            <p>Welcome, User! View your account info, credit, and orders.</p>
            {/* Add React components for user info and orders */}
        </div>
    );
} 